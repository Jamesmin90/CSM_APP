import 'package:flutter/material.dart';

class StudentBlog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Student Blog'),
      ),
      body: Text('TBA'),
    );
  }
}